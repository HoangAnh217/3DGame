using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleCollisionHandler : MonoBehaviour
{
   /* public GameObject explosionEffect; // Prefab hiệu ứng nổ

    private ParticleSystem particle;

    private void Start()
    {
        particle = GetComponent<ParticleSystem>();
    }
    private void OnParticleCollision(GameObject other)
    {
        GameObject obj=  Instantiate(explosionEffect, Vector3.zero, Quaternion.identity);
        obj.SetActive(true);
        // Đặt lifetime của hạt về 0 để hạt biến mất
    }
    private IEnumerator ExpolosionEffect()
    {
        return
    }*/
}
